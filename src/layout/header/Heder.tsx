import React from 'react';
import styled from "styled-components";

export const Header = () => {
    return (
        <StyledHeader>
meow
        </StyledHeader>
    );
};

const StyledHeader = styled.header `
    color: white;
`
