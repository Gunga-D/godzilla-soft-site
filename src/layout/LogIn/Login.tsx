import styled from "styled-components";

export const LogIn = () => {
    return (
        <StyledLogin>
           Вход в аккаунт!
        </StyledLogin>
    );
};

const StyledLogin = styled.div `
  margin-top: 300px;
  color: white;
`;
