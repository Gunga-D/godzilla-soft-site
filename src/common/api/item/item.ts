
export type Item = {
    id?: number,
    title?: string,
    platform?: string,
    region?: string,
    current_price?: number,
    is_for_sale?: string,
    old_price?: number,
    thumbnail_url?: string,
}
